import { AuthenticationForm } from "@/components/AuthComponents/Authentification/AuthenticationForm";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return <AuthenticationForm />;
};

export default page;
